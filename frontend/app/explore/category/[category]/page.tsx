import type{Metadata}from"next";import{ExploreLanding}from"../../../../components/ExploreLanding";import{getExploreDestinations}from"../../../../lib/api";
export async function generateMetadata({params}:{params:Promise<{category:string}>}):Promise<Metadata>{const{category}=await params,label=category.replaceAll("-"," ");return{title:`${label} near Appalachian trails | Explore Appalachia`,description:`Find approved ${label} destinations near off-road trails across Appalachia.`}}
export default async function CategoryPage({params}:{params:Promise<{category:string}>}){const{category}=await params,key=category.replaceAll("-","_");const rows=await getExploreDestinations(`category=${encodeURIComponent(key)}&limit=250`).catch(()=>[]);return <ExploreLanding title={category.replaceAll("-"," ")} description="Approved destinations near Appalachian trails, ready to add to your trip." destinations={rows}/>}
