import { ExplorePhotoForm } from "../../../../components/ExploreSubmissionForms"; export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main className="page"><section className="page-hero compact"><h1>Add a Photo</h1><p>Submit only photos you have permission to share. Every photo is moderated.</p></section><ExplorePhotoForm slug={slug}/></main>}
